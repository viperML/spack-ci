spack:
  view: false
  concretizer:
    unify: when_possible

  specs:
  - zstd
  - cmake
  # - python
  # - py-numpy
  # - npm

  packages:
    all:
      require: 'target=x86_64'

  mirrors:
    gh-buildcache:
      url: oci://ghcr.io/viperML/spack-ci
      signed: false
